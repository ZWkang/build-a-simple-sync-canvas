# Keep canvas access unprotected

The first product slice has no accounts or authorization: the home page lists every Canvas in the current installation, and anyone with a Canvas URL can edit it. The interface states this boundary explicitly so the example is not mistaken for a secured collaboration product.
